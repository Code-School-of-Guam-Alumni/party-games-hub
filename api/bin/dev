#!/usr/bin/env ruby
host = ENV.fetch("PARTY_GAMES_DEV_API_HOST", "127.0.0.1")
port = ENV.fetch("PARTY_GAMES_DEV_API_PORT", "43201")

exec "./bin/rails", "server", "--binding", host, "--port", port, *ARGV
